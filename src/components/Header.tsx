import React from 'react';

const Header = () => {
    return <header className="text-center py-6 text-2xl font-semibold border-b border-gray-700">
        CONNECT
    </header>;
};

export default Header;